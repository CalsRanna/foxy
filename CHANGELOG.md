# 更新日志

发布流水线读取最新一条版本段作为更新说明(`latest.yaml` 的 `notes`)。
发布前请为本版本补充 `## vX.Y.Z` 段。

## v1.0.1

- 新增：自动更新。设置页「关于与更新 → 检查更新」可手动检查；每次启动自动检查一次（24 小时内不重复）。
- 新增：设置页展示当前版本与项目主页入口。
- 更新策略：自动更新仅推送正式版本，跳过带预发布后缀的 tag（如 `v1.1.0-beta`）发布的测试版本。
