name: foxy
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  auto_route: ^9.2.2
  flutter_riverpod: ^2.5.1
  get_it: ^8.0.0
  hugeicons: ^0.0.7
  laconic:
    git:
      url: git@github.com:CalsRanna/laconic.git
      ref: main
  logger: ^2.4.0
  mysql_client: ^0.0.27
  package_info_plus: ^8.3.0
  path: ^1.9.1
  riverpod_annotation: ^2.3.5
  signals: ^6.0.2
  signals_flutter: ^6.0.2
  url_launcher: ^6.3.1
  window_manager: ^0.4.2
  yaml: ^3.1.3
  yaml_edit: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  auto_route_generator: ^9.0.0
  build_runner: ^2.4.9
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true

  assets:
    - asset/image/
